// English is the source locale. Natural-language keys keep existing copy readable
// while allowing future locale catalogs to override any message without changing UI code.
export const en = Object.freeze({
  translation: Object.freeze({
    'i18n.smoke': 'English',
    deviceCount_one: '{{count}} device',
    deviceCount_other: '{{count}} devices',
  }),
});
